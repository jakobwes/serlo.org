version: 2

vars:
  env_name: dev
  gcloud_env_name: serlo_dev
  env_dir: ../infrastructure/live/dev
  dump_location: gs://serlo_dev_terraform/sql-dumps/dump-2019-05-13.zip
  athene2_host: https://de.serlo.local/mathe

includes:
  build:     tasks/build.yml
  log:       tasks/log.yml
  cluster:   ../infrastructure/tasks/cluster_gcloud.yml
  terraform: ../infrastructure/tasks/terraform_gcloud.yml
  project:   tasks/project.yml

tasks:
  default:
    cmds:
      - task -l
